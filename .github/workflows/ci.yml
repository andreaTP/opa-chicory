name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# When a new revision is pushed to a PR, cancel all in-progress CI runs for that
# PR. See https://docs.github.com/en/actions/using-jobs/using-concurrency
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:

  prepare-testsuite:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout OPA
      uses: actions/checkout@v4
      with:
        repository: open-policy-agent/opa
        ref: main
        # ref: v0.67.1
        path: opa
    - name: Prep OPA cases
      working-directory: opa
      # TODO: This make target runs the tests, improve it
      run: make wasm-rego-test
    - uses: actions/upload-artifact@v4
      with:
        name: testcases
        path: opa/.go/cache/testcases.tar.gz

  build:
    dependsOn: prepare-testsuite
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java-version: [11, 17, 21]
        opa-version:
        - 0.67.1 # latest - verify how to upgrade it

    steps:
    - uses: actions/checkout@v4

    - uses: actions/download-artifact@v4
      with:
        name: testcases

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '${{ matrix.java-version }}'
        cache: maven

    - name: Install Open Policy Agent v${{ matrix.opa-version }}
      uses: open-policy-agent/setup-opa@v2
      with:
        version: ${{ matrix.opa-version }}

    - name: Test OPA-Chicory
      run: mvn spotless:apply clean install
